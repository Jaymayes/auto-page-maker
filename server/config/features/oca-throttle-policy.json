{
  "version": "1.0.0",
  "created_at": "2026-01-20T00:30:00Z",
  "canary_id": "oca_canary_5pct_20260120",
  "throttle_policy": {
    "enabled": true,
    "triggers": [
      {
        "id": "p95_approaching_limit",
        "condition": "p95 >= 1300ms for 10+ minutes",
        "action": "reduce_rate_limit",
        "new_rate_limit": 4,
        "emit_event": "oca_throttle_engaged",
        "notify": ["ops", "engineering"]
      }
    ],
    "recovery": {
      "condition": "p95 < 1000ms for 30 minutes",
      "action": "restore_rate_limit",
      "restored_rate_limit": 5,
      "emit_event": "oca_throttle_released"
    },
    "rate_limits": {
      "default": 5,
      "throttled": 4,
      "unit": "submissions_per_user_per_day"
    },
    "escalation": {
      "if_throttle_insufficient": {
        "condition": "p95 >= 1500ms after throttle",
        "action": "execute_kill_runbook"
      }
    }
  },
  "slo_thresholds": {
    "p95_target_ms": 1500,
    "p95_warning_ms": 1300,
    "p95_recovery_ms": 1000,
    "error_rate_max_pct": 1.0,
    "queue_depth_max": 30
  }
}
