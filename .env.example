# ScholarMatch Platform Environment Configuration
# Copy this to your secrets and configure the values

# Required for full functionality
OPENAI_API_KEY=your_openai_api_key_here
SHARED_SECRET=your_shared_secret_for_agent_bridge_here
JWT_SECRET=your_jwt_secret_for_authentication_here

# Optional development settings
NODE_ENV=development
PORT=5000

# CORS Configuration (strict allowlist-based enforcement)
CORS_ALLOWLIST=http://localhost:5000,https://your-replit-domain.replit.dev
CORS_ALLOW_CREDENTIALS=true
CORS_MAX_AGE=600
CORS_LOG_ONLY=false

# Agent Bridge Configuration (for Command Center integration)
AGENT_BASE_URL=https://your-agent-domain.replit.dev
AGENT_ID=scholarmatch-agent
AGENT_NAME=ScholarMatch Platform Agent
COMMAND_CENTER_URL=https://command-center.replit.dev

# Analytics (optional)
VITE_GA_MEASUREMENT_ID=G-XXXXXXXXXX

# Unicode normalization configuration (optional)
UNICODE_IDENTIFIER_FIELDS=email,username,slug,id,path,file,origin,city,state,major,category,type,name,title

# Database (automatically configured in Replit environment)
# DATABASE_URL=postgresql://user:pass@host:port/db